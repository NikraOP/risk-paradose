{
  "name": "afterfall",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mobile browser roguelite with top-down combat",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check docs/engine.mjs && node --check docs/renderer.js && node --check docs/game.js"
  }
}
